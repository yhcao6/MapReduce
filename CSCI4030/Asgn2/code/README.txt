To run the code:

1. Run "python preprocess/preprocess.py" to remove duplicate words each line

2. For part a, put preprocess input file in "part_a/input" directory, and then run "python a.py"

3. For part b, c, d, put preprocess files in home "input" directory, then run "run.sh"
